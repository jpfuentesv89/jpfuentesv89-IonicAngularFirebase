import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.web.veteriapps',
  appName: 'veteriApps',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
